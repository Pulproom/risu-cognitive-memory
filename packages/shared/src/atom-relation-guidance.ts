/** The same relation ontology and provenance requirements apply to generation
 * and review. Keep this independent of evaluation fixtures. */
export const ATOM_RELATION_GUIDANCE = `ATOM RELATIONS
- Preserve a concrete connection only when current source evidence establishes it. source is a detail emitted in this batch; target is another emitted detail or an exact atomRef from recallCandidates.atoms. Never invent atomRef or use raw IDs, older memory keys, or a memory synopsis as an endpoint.
- Store current/later source toward earlier target. Choose the kind that describes the evidenced connection, not merely its chronology. fulfillment_of means an explicit commitment was kept; resolution_of closes that open situation; contradiction_of negates that claim; consequence_of is its evidenced result. callback_to requires explicit reuse of a distinctive signal, phrase, action or object, not similar tone. same_referent requires evidence of the very same identifiable referent, not the same category or keyword. Omit generic thematic similarity.
- continuation_of requires current evidence of resumption or another stage of the same ongoing or interrupted event or activity. It is not a default for any later related fact. Use an explicitly supported kind above instead of continuation_of when it describes the connection more specifically. If the evidence does not distinguish competing kinds, omit the optional relation; do not use continuation_of to conceal uncertainty.
- Cite current source units that connect the facts, including source detail evidence. For two batch-local details, cite evidence overlapping both endpoints. Old atom snippets are lookup aids, not new evidence: the current source must establish the connection to the shown snippet. Do not infer omitted parts of a truncated snippet. Do not connect two old atoms.
- access is independently evidence-backed knowledge of the RELATION. Knowing both facts does not imply knowing their connection. Each holder must know both endpoint facts and the relation; otherwise omit that grant. Empty access is narrator-only. confidence is extraction confidence, never reinforcement energy. Uncertain or absent relations may be omitted; do not fill a quota.`;
