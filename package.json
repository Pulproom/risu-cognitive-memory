{
  "name": "risu-cognitive-memory",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.34.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "start": "pnpm --filter @rcm/server start",
    "dev": "pnpm --filter @rcm/server dev",
    "build:test": "pnpm --filter @rcm/plugin build"
  },
  "devDependencies": {
    "@types/node": "^22.19.3",
    "typescript": "^5.9.3"
  }
}
